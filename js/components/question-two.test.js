import React from 'react';
import {shallow, mount, render} from 'enzyme';

import Homepage from './question-two';

describe('<QuestionTwo />', () => {
    it('Renders without crashing', () => {
        shallow(<QuestionTwo />);
                });
    });
