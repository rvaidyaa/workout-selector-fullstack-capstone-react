import React from 'react';
import {shallow, mount, render} from 'enzyme';

import Homepage from './question-three';

describe('<QuestionThree />', () => {
    it('Renders without crashing', () => {
        shallow(<QuestionThree />);
                });
    });

