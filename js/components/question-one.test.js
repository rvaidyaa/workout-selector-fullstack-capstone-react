import React from 'react';
import {shallow, mount, render} from 'enzyme';

import Homepage from './question-one';

describe('<QuestionOne />', () => {
    it('Renders without crashing', () => {
        shallow(<QuestionOne />);
                });
    });

